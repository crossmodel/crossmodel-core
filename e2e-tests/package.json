{
  "name": "crossmodel-e2e-tests",
  "version": "0.7.5",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "author": {
    "name": "CrossBreeze",
    "email": "devops@crossbreeze.nl"
  },
  "scripts": {
    "build": "npm install && npm run clean && tsc --incremental && npm run playwright:install",
    "clean": "rimraf --glob lib *.tsbuildinfo .eslintcache",
    "lint": "eslint -c ../.eslintrc.js --ext .ts ./src",
    "playwright:install": "playwright install chromium --with-deps",
    "test": "",
    "ui-test": "npm run build && npm exec playwright test"
  },
  "dependencies": {
    "@eclipse-glsp/glsp-playwright": "2.4.0",
    "@playwright/test": "^1.47.0",
    "@theia/playwright": "1.64.1",
    "ts-dedent": "^2.2.0"
  },
  "devDependencies": {
    "allure-playwright": "^2.9.2"
  }
}
